0) Correct the project directories names, add common.h.c for conversion NTOHS NTOHL macro functions, reading raw data etc.
>>CHECK

1) Define endianness swap as macro in common.h
>>CHECK | not sure

2) Add structures to:
    - ethernet protocol
    - ip porotocol
    - udp protocol
>>CHECK

3) ./parses/udp_pack 

4) protocol headers should not be included to main function

5) void pointer casting on raw data package -> ???

6) define CONSTANTS to protocols frame sizes normally
>> CHECK


